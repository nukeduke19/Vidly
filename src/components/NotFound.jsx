import React from "react";
const NotFound = () => {
  return (
    <h1
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      NotFound
    </h1>
  );
};

export default NotFound;
